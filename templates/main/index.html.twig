{% extends 'base.html.twig' %}

{% block title %}Spotify Library{% endblock %}

{% block content %}
    <h1>Hello World</h1>

    {% if user %}
        <p>Your name is {{ user.name }}</p>
    {% else %}
        <p>You're not logged in.</p>
    {% endif %}


    {{ dump(userProvider.currentPlayback) }}
{% endblock %}
